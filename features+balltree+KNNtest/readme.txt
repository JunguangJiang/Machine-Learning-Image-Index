特征数据与助教给的imagelist.txt中的顺序一致。
label_feature.txt是神经网络的最后一层的输出，每张图片对应10维数据。
feature.txt是神经网络最后第二层的输出，每张图片对应512维数据。
feature_label.txt是对512维数据进行pca降维到32维后得到的数据。
检索系统需要尝试使用10维和32维数据进行检索，并比较检索准确度。512维数据可以选做。
